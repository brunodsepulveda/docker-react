sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t brunodsepulveda/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true brunodsepulveda/docker-react npm run test
